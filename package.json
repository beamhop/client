{
  "name": "beamhop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter='@beamhop/client' dev",
    "build": "bun run --filter='@beamhop/client' build",
    "start": "bun run --filter='@beamhop/client' start",
    "typecheck": "bun run --filter='*' typecheck",
    "test": "bun run --filter='*' test",
    "test:e2e": "bun run --filter='@beamhop/client' test:e2e"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.7.0"
  }
}
