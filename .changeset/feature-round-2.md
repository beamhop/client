---
"@verity/core": minor
---

Add NIP-50 full-text search (`searchNotes`, `searchProfiles`), a time-windowed
`RelayPool.collect` for streaming queries, and live relay management
(`NostrClient.setRelays`, `RelayPool.setRelays` now closes dropped sockets).
